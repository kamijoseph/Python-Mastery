
#nested functions cells

#num = input("Enter a whole positive number: ")
#num = float(num)
#num = abs(num)
#num = round(num)

#print(num)

#You can use the nested version instead

print(round(abs(float(input("Enter a whole poitive number: ")))))